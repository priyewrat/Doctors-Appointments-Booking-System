services:
  - type: web
    name: doctors-appointments-backend
    env: node
    plan: free
    rootDirectory: backend
    buildCommand: "npm install"
    startCommand: "npm run start"
    autoDeploy: true
    envVars:
      - key: MONGODB_URI
        sync: false
      - key: JWT_SECRET
        sync: false
